export default function AnnouncementBar() {
  return (
    <div className="bg-ink px-4 py-2.5 text-center text-[11px] leading-snug tracking-wide text-cream sm:text-xs">
      Free shipping on orders over $100 — new colorways just dropped
    </div>
  )
}
